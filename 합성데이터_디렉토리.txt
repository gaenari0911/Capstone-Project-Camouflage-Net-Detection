객체 분리해서 따로 저장
=> 배경 이미지 구해서, 




<시작 디렉토리 구조>
Dataset/
 ├── Training/
 │    ├── images/ (700)
 │    └── GT/     (700)
 └── Testing/
      ├── images/ (300)
      └── GT/     (300)

<목표 디렉토리 구조>
Dataset/
 ├── images/
 │    ├── train/ (8000) => (6000(위장망 O) + 2000(배경만 O <1400장(초록/갈색) + 600장(설원)>))  real + synthetic
 │    ├── val/ (100(위장망 O) + 30(배경만O <20장(초록/갈색) + 10장(설원)>)) real
 │    └── test/ (200(위장망 O) + 60(배경만O <40장(초록/갈색) + 20장(설원)>)) real
 │
 ├── labels/
 │    ├── train/
 │    ├── val/
 │    └── test/
 │
 ├── reserve/
 │    ├── hard_positive_pool/(위장망있는데 안보이는) => 가능하면 100장
 │    └── hard_negative_pool/(배경인데 위장망처럼 보이는) => 100장 따로
 │
 ├── reserve_labels/
 │    ├── hard_positive_pool/
 │    └── hard_negative_pool/
 │
 └── masks_raw/

<시작> 기존 데이터 정리 → 라벨 변환 → 분할 → 배경 수집 → 합성 생성 → 최종 구조 구성

배경 only이미지(총 2100장, 그 중 100장은 val_test인데
train 배경보다 많이 헷갈린 것 선별)는 위장망과 헷갈리게 생겨야함
(천, 낙엽 더미, 눈 반사, 인공 구조물, 패턴 많은 바닥)
배경 only이미지 2100장 => 2000장(Train)
			       => 30장(Val) => (20장(초록/갈색) + 10장(설원))
			       => 60장(Test) => (40장(초록/갈색) + 20장(설원))


<착각>
설원 배경과 초록색 위장망을 섞는건
현실적으로 절대 안된다고 생각했는데
만약, 설원 배경과 흰색 위장망만 섞으면
모델은 
if background == white:
    "밝은/흰 텍스처만 위장망"
else:
    "다른 색 텍스처 위장망"
이렇게 색과 텍스처를 결합해서 생각해버림

그러면 
눈 위에 흙이 묻거나
그림자때문에 어두워진 흰 위장망은
놓치거나(FN), 위장망 아닌 애를 위장망으로 잡아버림(FP)
